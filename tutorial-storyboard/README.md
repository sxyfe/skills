# tutorial-storyboard

把已认稿的口播脚本切成 `work/timeline.json`（host / screen / broll / gfx / sub）。不调 API，不渲染。

```bash
npx skills add sxyfe/skills@tutorial-storyboard -g -y
```

通常由 `/ai-host-tutorial` 在分镜阶段调用。
